import 'package:flutter/material.dart';

class SavedJokesScreen extends StatelessWidget {
  const SavedJokesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Saved Jokes Screen'),
    );
  }
}
